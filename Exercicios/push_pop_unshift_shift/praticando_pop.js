// Remova o ultimo elemento da array.

const jogos = ["The Last of Us", "Horizon", "Delta Force", "Ghost of Tsushima"];

jogos.pop();    // pop -> Remove o último elemento da lista.

console.log(jogos);

jogos.pop();

console.log(jogos);
