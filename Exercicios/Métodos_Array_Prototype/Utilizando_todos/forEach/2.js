// 2) Somar valores usando forEach (sem usar reduce).
// 🔧 Some todos com forEach e mostre o total.

const valores = [5, 7, 9];
let soma = 0;

valores.forEach((valor) => {
    soma += valor;
});

console.log(soma);

// concluido